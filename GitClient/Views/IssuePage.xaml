<Page
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
      xmlns:viewModels="clr-namespace:GitClient.ViewModels"
      x:Class="GitClient.Views.IssuePage"
      mc:Ignorable="d" 
      d:DesignHeight="766" d:DesignWidth="1310"
      Title="IssuePage" Width="Auto" Height="Auto" Background="White">
	<Page.DataContext>
		<viewModels:IssueViewModel/>
	</Page.DataContext>
    <DockPanel LastChildFill="False" Margin="0">
        <DockPanel Background="#FFECE5E5" Width="200" ScrollViewer.CanContentScroll="True" MinHeight="2">
			<ComboBox x:Name="AccountsComboBox" 
					  Height="51" 
					  ItemsSource="{Binding Users}"
					  VerticalAlignment="Top" 
					  Margin="30,50,30,0" HorizontalContentAlignment="Center" VerticalContentAlignment="Center" DockPanel.Dock="Top">
				<ComboBox.ItemTemplate>
					<DataTemplate>
						<StackPanel HorizontalAlignment="Center" 
									VerticalAlignment="Center" >
							<Label Content="{Binding Username}" FontWeight="Bold" FontSize="14" HorizontalAlignment="Center" VerticalAlignment="Bottom" HorizontalContentAlignment="Left" VerticalContentAlignment="Bottom"/>
							<Label Content="{Binding Provider}" FontWeight="Bold" FontSize="10" HorizontalAlignment="Center" VerticalAlignment="Top" HorizontalContentAlignment="Center" VerticalContentAlignment="Top"/>
						</StackPanel>
					</DataTemplate>
				</ComboBox.ItemTemplate>
				</ComboBox>
            <ListBox x:Name="RepositoriesListView" Margin="20,30" VerticalContentAlignment="Stretch" Padding="0" HorizontalContentAlignment="Stretch" Panel.ZIndex="2" DockPanel.Dock="Bottom">
                <ListBox.ItemTemplate>
                    <DataTemplate>
                        <StackPanel>
                            <Label FontWeight="Bold" FontSize="26.667"></Label>
                            <Label FontStyle="Normal" FontSize="16"></Label>
                        </StackPanel>
                    </DataTemplate>
                </ListBox.ItemTemplate>
            </ListBox>
        </DockPanel>
        <Frame x:Name="frame" DockPanel.Dock="Top">
            <Frame.Content>
                <ListView x:Name="listView" Height="750" VerticalAlignment="Top" Width="100">

                    <ListView.ItemTemplate>
                        <DataTemplate>
                            <StackPanel>
                                <Label FontSize="26.667" FontWeight="Bold"></Label>
                                <Label FontSize="16"></Label>
                            </StackPanel>
                        </DataTemplate>
                    </ListView.ItemTemplate>
                    
                </ListView>
            </Frame.Content>
        </Frame>
    </DockPanel>
</Page>
